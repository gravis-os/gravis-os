import React from 'react'
import BallotOutlinedIcon from '@mui/icons-material/BallotOutlined'
import { routes } from 'app/routes'

export const <%= props.module.name.camelCase %>FormSections = [
  {
    key: 'general',
    title: 'General',
    subtitle: 'Fill up general details',
    icon: <BallotOutlinedIcon />,
    fields: [
      { key: 'title', name: 'title', type: 'input', required: true },
      { key: 'subtitle', name: 'subtitle', type: 'input' },
    ],
  },
]

export const <%= props.module.name.camelCase %>Module = {
  Icon: BallotOutlinedIcon,
  sk: 'slug',
  table: {
    name: '<%= props.module.name.lowerSnakeCase %>',
  },
  name: {
    singular: '<%= props.module.name.startCase %>',
    plural: '<%= props.module.pluralName.startCase %>',
  },
  route: {
    plural: routes.<%= props.module.name.upperSnakeCase %>S
  },
}

export const <%= props.module.name.camelCase %>PreviewFormSections = <%= props.module.name.camelCase %>FormSections

export const <%= props.module.name.camelCase %>ColumnDefs = [{ field: 'title' }, { field: 'subtitle' }]
